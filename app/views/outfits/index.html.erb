<p id="notice"><%= notice %></p>

<h1>Outfits</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Stylist</th>
      <th>Shoes</th>
      <th>Trousers</th>
      <th>Shirt</th>
      <th>Integer checked</th>
      <th>Type</th>
      <th>Season</th>
      <th>Name</th>
      <th>Comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @outfits.each do |outfit| %>
      <tr>
        <td><%= outfit.user_id %></td>
        <td><%= outfit.stylist_id %></td>
        <td><%= outfit.shoe_id %></td>
        <td><%= outfit.trouser_id %></td>
        <td><%= outfit.shirt_id %></td>
        <td><%= outfit.integer_checked %></td>
        <td><%= outfit.outfit_type %></td>
        <td><%= outfit.season %></td>
        <td><%= outfit.name %></td>
        <td><%= outfit.comment %></td>
        <td><%= link_to 'Show', outfit %></td>
        <td><%= link_to 'Edit', edit_outfit_path(outfit) %></td>
        <td><%= link_to 'Destroy', outfit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Outfit', new_outfit_path %>
